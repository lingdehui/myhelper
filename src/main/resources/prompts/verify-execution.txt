判断以下执行是否真正达到了用户的要求。如果整体失败，指出哪些连续的步骤链可以作为独立技能复用。

用户请求: %s
执行步骤:
%s
AI最终回复: %s

校验规则:
- 如果任务实际完成了用户要求（网页打开了正确页面、文件创建成功等），返回 success=true
- 可续写的长任务（如写小说、分章创作）：只要本次已产出并保存部分有效进度（章节/大纲/人物已落盘），就返回 success=true（部分成功），不要因「未完成全部章节/全部目标」判失败
- 如果工具执行了但最终效果不对（如打开了错误网站、跳转了错误页面），返回 success=false
- 如果AI回复包含"无法"、"不能"、"没有"等否定词，返回 success=false

失败时分析 salvageableChains:
- 找出执行成功（✅）的连续步骤，判断它们是否可以形成独立可复用的子技能
- 例如：步骤0✅打开浏览器、步骤1✅输入网址 → 虽然总任务失败（网址错了），但"打开浏览器+输入网址"是可复用链 → salvageableChains: [[0,1]]
- 步骤索引从0开始，每个链是一组连续索引
- 单步也可以（如步骤3✅ OCR识别 → [[3]]）
- 没有可复用的就填 []

返回严格 JSON（不要 markdown 标记）:
{"success": true/false, "reason": "判断理由（20字内）", "salvageableChains": [[0,1], [3]]}
