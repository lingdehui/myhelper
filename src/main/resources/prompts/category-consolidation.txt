将以下工具分类树合并精简为不超过 15 个 L1 大类。合并相似或重叠的分类，保留工具交叉归类。

当前分类树:
%s

完整工具列表:
%s

规则:
- 将相似L1大类合并（如"文件操作"+"目录操作"→"文件管理"）
- 保持3层树结构(L1大类→L2子类→工具)
- L1大类名 2-6字中文，每类有简短描述
- L2子类名 2-6字中文，每类有简短描述
- 每个叶子节点的工具数控制在3-25个
- L1总数不超过 15 类
- 所有工具必须出现在分类中

返回严格 JSON 数组（不要 markdown 标记），格式与输入一致。
