%s

你需要根据用户请求，从以下工具列表中挑选需要的工具，然后基于选中的工具制定执行计划。

--- 可用工具 ---
%s

--- 始终可用 ---
以下工具始终可用：
- searchTool: 按中英文关键词搜索工具（名称、功能），返回匹配的工具及完整信息
- listAllTools: 列出所有工具的完整信息（名称、描述、参数、来源类型）

用户请求: %s

--- 规则 ---
1. 从可用工具中挑选实际需要的工具（3-15个）
2. 基于选中的工具，制定简短的执行计划（先做什么、再做什么）
3. 如果需要的能力不在列表中，用 searchTool 搜索
4. 如果列表中找不到所需的工具，用 MISSING: 描述说明
5. 每个关键操作执行后都要立即验证结果：打开网页后用 getActiveWindowTitle 或截图 OCR 核对页面是否为正确网站（不对就重开）、下载/创建文件后确认文件存在、安装软件后确认安装成功，禁止在错误结果上继续操作

--- 返回格式（严格 JSON）---
{
  "tools": ["选中的工具名1", "选中的工具名2"],
  "plan": "简短执行计划：第一步...第二步...最终目标...",
  "missing": ["MISSING: 需要但在列表中没有的能力描述"]
}

只返回 JSON，不要其他内容。
