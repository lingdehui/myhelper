# 09 - 待做任务

> 最后更新：2026-08-14

---

## 已完成

| # | 内容 | 状态 |
|---|------|:--:|
| #1 | **Rule 规则抽象层** — Neo4j `(:Rule)` 节点 + `RuleInductionService` 每天凌晨3点跨案例归纳 → 注入 AI 规划 prompt | ✅ |
| #2 | **失败经验主动注入规划** — `ToolPlanner.plan()` 并行检索 FailurePattern 作为 warning 注入 prompt | ✅ |
| #5 | **生成工具监控淘汰** — `@GeneratedTool` 注解 + `LoggingToolCallback` 失败检测 + `GeneratedToolRegistry` 连续3次失败自动删除（源码+class+运行时卸载） | ✅ |
| #6 | **解耦 MyHelperApplication** — 抽取 `TurnProcessor` 执行管线（~420行），DBA 从 ~1250行 缩减到 ~790行 | ✅ |
| #7 | **自主探索引擎** — 空闲检测 + LLM决策 + 多方法执行 + 自我评估 + 记忆维护 | ✅ |
| #8 | **探索全量使用本地模型** — `AutonomousExplorationService` 使用 `modelRouter.exploration()` → Ollama qwen2.5:32b | ✅ |
| #9 | **@Scheduled 定时探索触发** — 每60秒巡检，串行锁 `ReentrantLock.tryLock()` 避免并发抢屏幕 | ✅ |
| #10 | **探索管线复用** — 学习目标送入 `TurnProcessor.processExploration()` 主执行管线 | ✅ |
| #11 | **cron 表达式修复** — `MemoryMaintenanceService` 从 SpEL `#{}` 改为标准 `${}` 属性占位符 | ✅ |
| #12 | **getMemoryUsageStatus 修复** — 从误调 `cleanup()` 改为只查询不清理 | ✅ |
| #16 | **能力清单动态化** — `CAPABILITY_RULES` 正则 + `buildDynamicCapabilities()` 启动扫描自动归类 | ✅ |
| #17 | **渐进式学习** — 难度分级 ⭐~⭐⭐⭐⭐ + `classifyDifficulty()` + `computeMissingBasics()` 强制基础能力 | ✅ |
| #18 | **三层SKIP防线** — 候选任务编号 + Prompt规则 + Java代码兜底，防止AI惰性SKIP | ✅ |
| #19 | **探索串行化** — `ReentrantLock` 保证同一时间只有一个探索会话 | ✅ |
| #20 | **中英双语工具搜索** — `ToolSearchService.searchTool(kw, kwEn)` 中文搜不到英文兜底 | ✅ |
| #21 | **@GeneratedTool 注解** — 自定义注解标识AI生成工具，供失败清除判断 | ✅ |
| #22 | **运行时即时加载** — `GeneratedToolRegistry.loadAtRuntime()` URLClassLoader 动态加载生成工具，无需重启 | ✅ |
| #23 | **万能搜索工具门禁与分页** — `listAllTools(page)` 默认锁死，仅分类降级场景放行（轮次用尽/AI不选分类/工具>50），每页50分页；`PlanResult.allowListAllTools` 信号贯穿规划→执行 | ✅ |
| #24 | **searchTool 升级向量搜索** — 从子串匹配升级为 Qdrant 语义向量搜索（limit=5, threshold=0.3）+ 中文→英文兜底 + 子串退化 | ✅ |

### v1.7 Unit 图化重构（文档 15）

| # | 内容 | 状态 |
|---|------|:--:|
| #25 | **Unit 统一对象** — 计划/步骤/工具统一抽象为 `Unit`，`unitKind`: TOOL / PLAN_STEP / MCP_TOOL | ✅ |
| #26 | **UnitStore 双写 + 语义检索** — Neo4j 主存储 + Qdrant `unit-registry`（embedding 只对 matchText 生成），最终一致性 + 补偿任务 | ✅ |
| #27 | **UniversalUnitExecutor 统一执行器** — `executePlanStep(unitId, paramsJson)` 三分支：原子直调 / 脚本化 / 递归 CONTAINS | ✅ |
| #28 | **UnitSedimentationService 差异更新沉淀** — 语义匹配去重 → 完全相同 / 某步不同 / 差异大 三分支 | ✅ |
| #29 | **UnitFailureService + FailureCause 复用** — FailureCause 独立节点（PLAN/ENVIRONMENT 归类），PLAN 失败 3 次 → ARCHIVED + unindex + DISABLES | ✅ |
| #30 | **脚本化判定 §7.2 + tryUpgradeScriptable** — 无递归子步骤 + 无数据流依赖 + 步骤数≤5（或 success≥5 && stability>0.9）+ 无 FALLBACK/DISABLES | ✅ |
| #31 | **探索同构 §9** — 探索任务声明 VALIDATE/OPTIMIZE，追加 ExplorationRecord，复用同一套 Unit 执行管线 | ✅ |

---

## P1 — 本周待办

### #3 Episode 过期淘汰 🟡

**问题**：Episode 没有时间维度的淘汰机制。旧的 Episode 即使环境变了也可能被匹配到。

**方案**：

| 时间 | 操作 |
|------|------|
| ≥90天未检索使用 | ACTIVE → archived=true，降低 stability |
| ≥180天 | 删除或移入冷存储 |
| archived 被再次匹配并成功 | 重置为 ACTIVE + 更新 last_used |

**现状**：`MemoryMaintenanceService.cleanup()` 已升级为价值评分版（基于 `timestamp` 的 ageDays/retentionDays），但 `lastUsedAt` 字段与"≥90天未用归档"仍未实现。

**改动点**：
- `Episode` 新增 `lastUsedAt` 字段
- `EpisodeCacheService` 新增 `@Scheduled` 淘汰任务（每周凌晨）
- `findSimilarEpisode()` 命中时更新 `lastUsedAt`

---

## P2 — 低优先级

### #4 ATOMIC 步骤链主动注入 🔵

**问题**：`UnitLearner` 能从 COMPOSITE 中提取 ATOMIC 通用步骤，但没有主动注入到 AI 规划中。

**方案**：在 `ToolPlanner.plan()` L3 AI 规划时，把与当前工具列表相关的 ATOMIC 作为"可用子流程"注入。

---

### #13 KnowledgeSnippet 持久化 🔵

**问题**：`ExplorationExecutor.saveKnowledgeSnippet()` 只有 TODO 注释，知识片段生成后只打印日志，未实际持久化到 Qdrant/Neo4j。

**方案**：创建 `KnowledgeSnippetService`，将 `KnowledgeSnippet` 存入 Qdrant `knowledge-snippets` collection + Neo4j `KnowledgeSnippet` 节点。供后续检索复用。

---

### #14 探索上下文增强 🟡（部分完成）

**问题**：`AutonomousExplorationService.buildContext()` 中 `{unsolved_questions}` 和 `{tool_list_with_usage}` 为硬编码占位。

**进度**：
- ✅ `tool_list_with_usage`：已改为 `buildToolOverview()` 按前缀分组生成真实工具概览
- ❌ `unsolved_questions`：仍硬编码 `"暂无记录"`

**剩余方案**：
- `unsolved_questions`：扫描近7天所有 FAILED Episode 的 failureLesson，聚合为未解决问题

---

### #15 Neo4j 记忆清理 🔵

**问题**：`MemoryMaintenanceService.cleanup()` 中有 TODO——"清理 Node4j 中的低价值 FailurePattern"，未实现。

**现状**：Episode 清理已升级为价值评分版，但 Neo4j `FailurePattern` 清理仍未实现。

**方案**：定期扫描 Neo4j `FailurePattern` 节点，删除 `count < 3` 且超过 180 天未更新的模式。

---

## 变更速查

| 新增文件 | 位置 | 用途 |
|----------|------|------|
| `TurnProcessor.java` | `service/` | AI 对话执行管线（从 DBA 抽离） |
| `RuleNode.java` | `memory/graph/` | Neo4j `(:Rule)` 节点实体 |
| `RuleRepository.java` | `memory/graph/` | 规则查询 Repository |
| `RuleInductionService.java` | `service/` | `@Scheduled` 跨案例规则归纳 |
| `FailurePatternTool.java` | `service/` | AI 可调用的失败经验检索 @Tool |
| `FailurePatternNode.java` | `memory/graph/` | Neo4j `(:FailurePattern)` 节点 |
| `FailurePatternRepository.java` | `memory/graph/` | 失败模式 Repository |
| `AutonomousExplorationService.java` | `exploration/` | 自主探索主控：动态能力清单+渐进学习+三层SKIP防线+串行锁+双语搜索 |
| `IdleDetectionService.java` | `exploration/` | 空闲条件检测（当前跳过） |
| `ExplorationTool.java` | `exploration/` | AI 可调用探索工具：手动触发+记忆状态查询 |
| `MemoryMaintenanceService.java` | `exploration/` | 记忆维护：@Scheduled 凌晨清理低价值数据 |
| `ExplorationDecision.java` | `exploration/` | 探索决策 record |
| `GeneratedTool.java` | `generated/` | `@GeneratedTool` 注解（标识AI生成工具） |
| `ToolSearchService.java` | `service/` | 万能搜索 `searchTool(kw, kwEn)` + `listAllTools(page)` 门禁分页 |
| `LoggingToolCallback.java` | `service/` | 工具调用包装器（含失败检测上报+生成工具自动清理） |
| `myhelper-exploration.yml` | `config/` | 探索引擎 + 记忆维护配置 |

| 重大修改 | 说明 |
|----------|------|
| `ToolPlanner.PlanResult` | 新增 `failureWarnings`、`allowListAllTools` 字段 |
| `ToolPlanner.plan()` | L1/L2 并行调用 `searchFailurePatterns()` |
| `ToolPlanner.doPlan()` | 注入失败警告 + 活跃规则到 AI prompt |
| `MyHelperApplication` | ~1250行→~790行，AI 逻辑移交 TurnProcessor；单bean独立provider；注册 ExplorationTool |
| `MyHelperConfig` | 新增 `modelRouter` Bean（exploration → qwen2.5:32b） |
| `MyHelperProperties` | 新增 Exploration/MemoryMaintenance records |
| `EpisodeCacheService` | 新增 `saveFailurePattern()` / `searchFailurePatterns()` / `getRecentLearnedTopics()` / `getRecentlyAttemptedTopics()` 等 |
| `DialogStateMachine` | 新增 `touch()` / `getLastInteractionTime()` 供空闲检测 |
| `PromptLoader` | 新增 `getAutonomousExploration()` 加载探索 prompt |
| `GeneratedToolRegistry` | 新增 `loadAtRuntime()` 即时加载 + `reportToolSuccess/Failure()` 3次失败自动清除 + `getDynamicTools()` |
| `AutonomousExplorationService` | 完全重写：动态能力清单 + 渐进学习 + 三层SKIP防线 + ReentrantLock串行 + 双语搜索提示注入 |
