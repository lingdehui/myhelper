# 可复用计划模板库设计文档

## 1. 背景与问题

当前系统（DesktopBrain）中的 Episode 经验闭环已经实现了对**扁平工具调用序列**的复用：
- 用户输入 → 三层缓存规划（内存/向量/AI规划）→ 执行 → 反思 → 存入 Qdrant Episode
- 相似请求可通过向量检索命中历史 Episode，直接回放工具序列（脚本化）或参考执行

但存在两个局限：

1. **计划结构扁平**：Episode 中存储的是 `List<ToolCallLog>`，即线性工具调用序列。没有显式的“步骤”概念，无法表达“计划包含步骤，步骤可嵌套子步骤”的分形结构。
2. **复用粒度单一**：只能复用整个 Episode（整个工具序列），难以复用其中的某个子步骤或子计划。

用户的核心设想是：**计划和步骤本质是同一个抽象，只是粒度不同**。计划就是大步骤，步骤就是小计划，二者都可以有自己的目标、步骤和工具调用，形成可嵌套的树形结构。

## 2. 设计目标

1. 引入显式的 `Step` 抽象，使计划可以递归包含子步骤和工具调用。
2. 将这种结构化的计划作为**可复用资产**存入数据库（Qdrant + Neo4j），支持通过自然语言向量搜索检索相似计划。
3. 检索到的计划交给 AI 评估：是否可以直接使用、需要修改、还是仅作参考。
4. 与现有 Episode 系统兼容，逐步从扁平结构迁移到嵌套结构。
5. 执行器支持递归执行嵌套计划，保留变量替换、失败重试、分段继续等现有能力。

## 3. 核心概念

| 概念 | 定义 |
|------|------|
| **Plan** | 顶层计划，与现有 `Episode` 类似，但包含结构化步骤而非扁平工具列表 |
| **Step** | 计划的一个步骤，可包含子步骤（List<Step>）和工具调用（List<ToolCallLog>） |
| **AtomicStep** | 只包含工具调用的叶子步骤，不再嵌套子步骤 |
| **PlanTemplate** | 从多个执行成功的 Plan 中提取的通用计划模板，参数化变量，可被直接复用 |
| **PlanLibrary** | 存储所有 Plan / Step / Template 的数据库集合 |

## 4. 数据模型

### 4.1 Step 结构（递归）

```java
public record Step(
    String id,                  // UUID
    String goal,                // 本步骤的目标描述
    List<Step> subSteps,        // 子步骤（可递归）
    List<ToolCallLog> toolCalls, // 本步骤直接调用的工具（叶子步骤使用）
    Map<String, String> params,  // 参数签名（变量名 → 来源/默认值）
    String successLesson,        // 本步骤的成功经验
    String failureLesson,        // 本步骤的失败教训
    int maxRetries,             // 本步骤重试次数
    boolean optional,           // 是否可选步骤
    long timeoutMs               // 超时时间
) {}
```

- 当 `subSteps` 非空时，`toolCalls` 通常为空（计划步骤）；反之亦然（原子步骤）。允许两者都存在的情况很少见，但为了灵活性可以保留。

### 4.2 Plan 结构（扩展 Episode）

```java
public class StructuredEpisode {
    private String id;                 // Qdrant point ID (UUID)
    private String userInput;          // 用户原始输入
    private String normalizedKey;      // 缓存 key
    private List<Step> steps;          // 计划步骤（树形结构）
    private List<ToolCallLog> flatLog; // 执行时的扁平工具调用日志（用于回溯和调试）
    private String successLesson;      // 整体成功经验
    private String failureLesson;      // 整体失败教训
    private Map<String, String> signature; // 顶层变量签名
    private int successCount;
    private int failureCount;
    private double stability;
    private boolean canScript;
    private EpisodeStatus status;
    private Instant timestamp;
    // ... 其他现有字段
}
```

- `steps` 是结构化的执行计划，`flatLog` 是实际执行的扁平记录（保留用于日志和现有功能的兼容）。

### 4.3 存储方案

| 数据库 | 用途 |
|--------|------|
| **Qdrant** | 存储 Plan 的向量化表示，用于自然语言语义检索 |
| **Neo4j** | 存储 Plan 与 Step 之间的图谱关系（如 `CONTAINS_STEP`, `SIMILAR_TO`），便于图查询和关系分析 |
| **关系型数据库（可选）** | 如果未来需要事务性更强的存储，可迁移到 PostgreSQL |

Qdrant 集合：`structured-plans`（或沿用 `episodes` 并扩展 payload）
- Payload 包含：`userInput`, `normalizedKey`, `stepsJson`（序列化后的树形结构）, `signature`, `successCount`, `stability`, `status` 等
- 向量：使用 `userInput` 或其语义摘要的 embedding

Neo4j 节点：
- `(:Plan {id})` -[:CONTAINS_STEP]-> `(:Step {id})`
- `(:Step)` -[:CONTAINS_STEP]-> `(:Step)`（递归关系）
- `(:Step)` -[:CALLS_TOOL]-> `(:ToolNode)`（工具节点）
- 可选：`(:Plan)` -[:SIMILAR_TO]-> `(:Plan)` 用于相似度关系

## 5. 核心工作流

### 5.1 计划创建

当 AI 规划新任务时，不再输出扁平的 `{tools: [...]}`，而是输出结构化的步骤树：

```json
{
  "steps": [
    {
      "goal": "打开微信并进入聊天窗口",
      "sub_steps": [
        {"goal": "启动微信", "tools": [{"tool": "launchApp", "args": {"app": "微信"}}]},
        {"goal": "搜索联系人", "tools": [{"tool": "findFriend", "args": {"name": "$contact"}}]}
      ]
    },
    {
      "goal": "发送消息",
      "tools": [{"tool": "typeText", "args": {"text": "$message"}}],
      "depends_on": [0]
    }
  ]
}
```

- 工具调用时记录为 `ToolCallLog`，同时保留在对应 Step 的 `toolCalls` 中。
- 执行成功后，整个计划（含步骤树）存入 Qdrant/Neo4j，进入 PlanLibrary。

### 5.2 向量检索与复用

用户输入 → `embeddingService.embed(userInput)` → Qdrant `structured-plans` 集合向量检索 → 返回 Top-N 相似计划（附 score）。

检索流程与现有 Episode 类似，但检索到的对象是结构化计划。

### 5.3 AI 评估与适配

检索到候选计划后，调用 AI 进行三步评估：

1. **适用性判断**：该计划是否适用于当前用户请求？
   - AI 输出：`{"applicable": true/false, "reason": "..."}`
2. **变量提取**：从当前用户输入中提取计划需要的变量值。
   - AI 输出：`{"variables": {"$contact": "张三", "$message": "你好"}}`
3. **执行模式选择**：
   - 若计划稳定（`stability > 0.9 && successCount >= 5`）且变量齐全 → **脚本化执行**（直接按步骤树执行，零 token）
   - 否则 → **带参考执行**：将计划的 goal/步骤/经验注入 prompt，让 AI 参考并执行

这实际上增强了现有 `PlanMatcher` 的能力，使其能处理树形结构。

### 5.4 脚本化执行（递归）

`PlanExecutor` 需要改造为递归执行步骤树：

```java
public ExecutionResult executeSteps(List<Step> steps, Map<String, String> variables, ToolCallback[] tools) {
    List<ToolCallLog> executed = new ArrayList<>();
    for (Step step : steps) {
        // 变量替换
        Map<String, Object> resolvedParams = resolveVariables(step.params(), variables);

        // 如果步骤包含子步骤，递归执行
        if (!step.subSteps().isEmpty()) {
            ExecutionResult subResult = executeSteps(step.subSteps(), variables, tools);
            executed.addAll(subResult.executedSteps());
            if (!subResult.success()) {
                return ExecutionResult.failed(executed, subResult.failedStepIndex(), subResult.errorMessage());
            }
        }

        // 执行本步骤的工具调用
        for (ToolCallLog toolCall : step.toolCalls()) {
            // 查找工具，调用，记录日志...
            // 失败时根据 maxRetries/optional 决定是否继续或中止
        }
    }
    return ExecutionResult.success(executed);
}
```

变量作用域：子步骤可以访问父步骤定义的变量（变量提升），也可以在步骤内部定义新变量。

### 5.5 计划模板提取（PlanTemplate）

类似于现有 UnitLearner，但提取的是**步骤子树**而非工具子序列。

- 收集多个成功执行的 Plan（structured episodes）。
- 挖掘频繁出现的公共步骤子树（如“打开应用→搜索→输入”）。
- 将公共子树参数化（将具体值替换为变量，如 `$app_name`, `$search_query`），生成 PlanTemplate。
- 模板存入 Qdrant 的 `plan-templates` 集合，可被直接复用。

## 6. 与现有系统的集成

| 现有组件 | 改动 |
|----------|------|
| `Episode` | 新增 `steps` 字段（List<Step>），保留原 `toolCalls` 作为兼容 |
| `PlanExecutor` | 支持递归执行 `steps`；保留原扁平执行逻辑作为 fallback |
| `PlanMatcher` | 升级为处理树形计划，输出变量和适用性 |
| `UnitLearner` | 从“提取工具子序列”升级为“提取步骤子树” |
| `ToolPlanner` | 规划 prompt 改为输出结构化步骤树；L3 规划结果解析调整 |
| `EpisodeCacheService` | 支持存储和检索结构化计划（`structured-plans` collection） |
| `QdrantConfig` | 新增 `structured-plans` 和 `plan-templates` 集合初始化 |

迁移策略：
- 保留现有 `episodes` 集合不变（用于扁平兼容）。
- 新建 `structured-plans` 集合，新计划使用树形结构存储。
- 旧的扁平 Episode 可通过 UnitLearner 的升级版逐步转换为结构化模板。

## 7. 实施路线图

| 阶段 | 内容 | 说明 |
|:--:|------|------|
| **P0** | 定义 `Step` 数据类，扩展 `Episode` | 纯数据模型，无行为改动 |
| **P1** | 改造 `ToolPlanner` 规划 prompt，输出步骤树 JSON | 核心改动，需要设计新的规划 prompt |
| **P2** | 改造 `PlanExecutor` 支持递归执行 | 保留扁平执行作后备 |
| **P3** | 新增 `structured-plans` 集合，扩展 `EpisodeCacheService` | 存储与检索 |
| **P4** | 升级 `PlanMatcher` 处理树形计划 | 变量提取和适用性判断 |
| **P5** | 升级 `UnitLearner` 提取步骤子树 | 模板化复用 |
| **P6** | 全面测试与性能优化 | 包括递归深度限制、变量作用域等 |

## 8. 风险与注意事项

- **递归深度控制**：步骤树可能过深，设置最大深度（如 5 层），防止无限递归。
- **AI 规划输出的稳定性**：让 LLM 输出严格 JSON 的树形结构较难，需要多次校验和降级策略（扁平 fallback）。
- **存储体积**：树形结构序列化后可能比扁平列表更大，注意 payload 大小限制。
- **兼容性**：确保旧版扁平 Episode 仍能正常检索和执行，不可破坏现有功能。

## 9. 总结

本设计将“计划=步骤”的分形思想落为可存储、可检索、可复用的**结构化计划库**。它是对现有 Episode 系统的自然演进，既保留了已有的经验学习能力，又引入了更细粒度的步骤复用和嵌套计划支持。通过向量检索 + AI 评估，系统能够更智能地复用历史计划，逼近用户设想的“越用越聪明”。
